
version: "3.9"
services:
  api:
    build: ./backend
    ports: ["8000:8000"]
  web:
    build: ./frontend
    ports: ["5173:5173"]
    environment:
      - VITE_API_BASE=http://localhost:8000
    depends_on: [api]

environment:
  - ADE_API_KEY=${ADE_API_KEY}
